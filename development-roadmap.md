# Rising Path 开发日程

> 当前开发策略已调整为“内测优先、商业化预留”。具体范围、额度模型、支付边界、验收标准与执行顺序见 [docs/internal-beta-release-plan.md](docs/internal-beta-release-plan.md)。本文件中原有的功能阶段作为能力清单保留；后续开发必须以该内测计划的阶段 A-E 为优先级，不再按原阶段编号顺序直接推进。

## 原始能力路线（供内测范围取舍）

先打通一条稳定、可验证的核心路径：

```text
注册/登录
  -> 上传简历
  -> 解析求职画像
  -> 用户确认画像
  -> 目标岗位匹配与简历评分
  -> 简历优化
  -> 模拟面试
  -> 投递记录
  -> 反馈更新画像
```

开发顺序遵循：先数据和主流程，再 AI 效果，再后台运营，最后做规模化优化。

## 开发规则

- 每完成一个阶段，先验收再进入下一阶段。
- 不在主链路未稳定时同时开发大量视觉效果。
- 所有 AI 结果必须保存来源、版本和用户确认状态。
- 所有后端接口必须从认证会话获取 `user_id`，不能信任客户端传入值。
- 新功能必须先定义成功标准，再开始写页面。
- 每次提交只完成一个明确目标，避免大范围混合修改。

## 原始阶段总览

| 阶段 | 目标 | 预计投入 |
|---|---|---|
| 0 | 项目基线和环境整理 | 1-2 天 |
| 1 | 认证和账号闭环 | 2-3 天 |
| 2 | 简历上传与求职画像 | 5-7 天 |
| 3 | 岗位匹配和简历评分 | 4-6 天 |
| 4 | 简历优化闭环 | 4-6 天 |
| 5 | 模拟面试闭环 | 5-7 天 |
| 6 | 网申与投递记录 | 4-6 天 |
| 7 | 管理员后台 | 5-7 天 |
| 8 | 反馈闭环、性能和上线 | 5-7 天 |

这是相对顺序，不是硬性承诺。任何阶段如果验收不通过，都应该先修复而不是继续堆新功能。

## 阶段 0：项目基线

### 目标

确认当前代码、数据库、环境变量、开发服务和部署方式处于可重复状态。

### 工作项

- 确认 `pnpm install`、`pnpm dev`、`pnpm run ts-check`、`pnpm run build` 可执行。
- 检查 Supabase 本地和生产环境变量是否分离。
- 检查 `auth.users`、`profiles`、`resumes`、`jobs` 等核心表和 RLS。
- 确认 `/login`、`/home`、`/jobs`、`/resume` 路由可访问。
- 确认 3D 资源不进入 Git 临时缓存，`card.glb` 和 `lanyard.png` 能返回 200。
- 阅读并保留 `docs/rising-path-system-blueprint.md` 和 3D 排障文档。

### 验收标准

- 新开发者按照 README 能在本地启动项目。
- 登录页、开发测试按钮、3D 弹窗可以正常打开。
- 类型检查通过，构建问题有明确记录。
- 不存在误提交的 `.next`、日志、临时文件和敏感环境变量。

## 阶段 1：认证和账号闭环

### 目标

让用户能够可靠地注册、验证、登录、退出和重置密码。

### 工作项

- 邮箱密码注册。
- 注册邮箱验证码验证。
- 普通邮箱验证码登录。
- Google OAuth。
- 密码重置。
- 未验证用户的拦截和重发验证码。
- 登录状态恢复和退出。
- 限流、Turnstile 和错误提示。

### 关键规则

- 注册提交不等于注册成功。
- 只有注册验证码通过并建立会话后，才展示注册成功页面。
- 普通邮箱验证码登录不应触发注册成功动画。
- Supabase 如果直接返回 session，服务端必须报告邮箱确认配置错误，不能绕过验证。

### 验收标准

- 新邮箱注册后进入验证码页面。
- 错误验证码不会建立会话，也不会展示成功弹窗。
- 正确验证码建立会话并展示成功动画。
- 关闭成功动画后进入 `/home`。
- 未验证用户不能访问受保护数据。

## 阶段 2：简历上传与求职画像

### 目标

把简历变成后续所有功能共享的结构化求职画像。

### 工作项

- PDF、DOCX 简历上传。
- 文件类型、大小和病毒风险校验。
- 原始文件存储和访问权限。
- 简历文本解析。
- 教育、经历、项目、技能和语言提取。
- 目标职位、目标地区、工作权限和毕业时间提取。
- 画像置信度和证据片段。
- 用户确认和手动修正。
- 画像版本保存。

### 画像字段分层

```text
证据层：原始文本、经历条目、项目成果
结构层：教育、技能、地区、时间、经历字段
策略层：求职阶段、优势、短板、方向和地区策略
```

### 验收标准

- 用户上传后能看到清晰的解析进度。
- 解析失败可以重试，不会生成半成品画像。
- 每个核心判断可以查看来源证据。
- 用户能修改画像，并看到修改后的确认状态。
- 画像未确认前，不给出最终岗位推荐结论。
- 数据按用户隔离，其他用户无法读取。

## 阶段 3：岗位匹配和简历评分

### 目标

让系统根据“用户画像 + 目标岗位”给出可解释的推荐和评分。

### 工作项

- 岗位筛选和搜索。
- 岗位标签标准化。
- 用户画像与岗位要求匹配。
- 匹配证据和关键差距。
- 目标岗位绑定的简历评分。
- 评分维度和权重配置。
- 评分快照和历史版本。
- 收藏、忽略和投递意向。

### 建议评分维度

- ATS 可解析性。
- 岗位关键词匹配。
- 经历与岗位要求匹配。
- 成果量化程度。
- 证据强度。
- 地区表达策略。
- 用户画像适配度。

### 验收标准

- 同一份简历针对不同岗位得分不同。
- 每个分数都能解释原因，不展示无依据的数字。
- 用户能看到最需要修复的三个问题。
- 评分绑定岗位和画像版本。
- 分数不会因为页面刷新而无故变化。

## 阶段 4：简历优化闭环

### 目标

让用户能看到优化前后差异，并知道为什么优化有效。

### 工作项

- 根据目标岗位生成优化策略。
- 根据地区规则调整表达。
- 优化项目成果和量化描述。
- 保留原始简历版本。
- 生成优化版本。
- 优化前后评分对比。
- 用户逐条接受、拒绝或编辑建议。
- 导出 PDF/DOCX。

### 验收标准

- 优化不会捏造用户不存在的经历。
- 每条建议都说明对应的岗位要求或画像短板。
- 用户可以撤销单条修改。
- 新版本评分可以复算并保存。
- 用户明确确认后才将版本用于投递。

## 阶段 5：模拟面试闭环

### 目标

把企业 DNA、岗位要求和用户画像结合成有针对性的面试训练。

### 工作项

- 企业 DNA 查看和版本管理。
- 面试官风格和轮次配置。
- 单轮面试和多轮面试。
- 基于用户画像的追问。
- 对话记录和流式响应。
- 面试报告、评分和短板。
- 低真实度反馈审核。
- 将面试短板转为简历或练习任务。

### 验收标准

- 面试问题与目标公司、岗位和用户经历相关。
- AI 不会使用用户简历中不存在的经历进行追问。
- 面试报告能引用对话证据。
- 评分保存企业 DNA 版本和画像版本。
- 用户反馈能进入管理员审核队列。

## 阶段 6：网申与投递记录

### 目标

让用户从“推荐岗位”自然进入“准备材料、填写字段、记录结果”。

### 工作项

- 岗位收藏和投递状态。
- 简历版本绑定岗位。
- 网申字段结构化。
- 历史字段建议。
- 用户确认后自动填充。
- 提交时间、状态和备注。
- 失败重试和人工补充。

### 验收标准

- 每次投递都能查到使用的岗位、简历版本和画像版本。
- AI 推断字段必须明确标记并由用户确认。
- 自动填写失败不会丢失用户输入。
- 用户能看到完整的投递进度。

## 阶段 7：管理员后台

### 目标

让运营人员能发现异常、修正数据、管理内容并追踪 AI 质量。

### 页面顺序

```text
/admin
/admin/users
/admin/resumes
/admin/jobs
/admin/company-dna
/admin/interviews
/admin/feedback
/admin/audit-logs
/admin/settings
```

### 优先功能

- 数据总览。
- 用户搜索和账号状态管理。
- 简历解析失败和画像低置信度审核。
- 岗位导入、去重、上下架。
- 企业 DNA 编辑和版本。
- 面试真实度反馈审核。
- AI 调用错误、成本和延迟监控。
- 角色权限和审计日志。

### 验收标准

- 管理员 API 在服务端验证角色。
- 不同管理员角色只能访问授权模块。
- 所有高风险修改都有操作者、时间和前后值。
- 用户隐私数据默认脱敏。
- 删除和封禁操作需要二次确认。

## 阶段 8：反馈闭环、性能和上线

### 目标

让系统可以稳定运行，并能根据真实使用结果持续改进。

### 工作项

- 记录收藏、忽略、投递、面试和最终结果。
- 建立用户求职漏斗。
- 监控解析成功率、AI 错误率、匹配点击率和投递转化率。
- 3D 组件懒加载、资源缓存和移动端适配。
- API 限流、日志、错误告警和备份。
- 生产环境 Supabase、邮件、OAuth、AI 和存储配置。
- 生产构建、回滚和数据库迁移流程。

### 上线验收标准

- 注册、验证、登录、上传、画像、匹配、优化和面试主链路完整通过。
- 手机和桌面端主要页面可用。
- RLS、管理员权限和敏感数据检查通过。
- 生产环境邮件链接和 OAuth 回调地址正确。
- 关键接口有失败提示和重试机制。
- 有可执行的回滚方案。

## 每周推进方式

每个开发周期固定使用以下节奏：

### 开始前

- 明确本周期唯一目标。
- 列出要改的页面、接口、表和验收条件。
- 确认不包含哪些功能，防止范围失控。

### 开发中

- 先完成数据和接口，再完成页面。
- 每完成一个接口就做成功、失败、未登录三种测试。
- 每完成一个页面就做桌面端和移动端检查。
- 所有 AI 输出保留原始输入、版本和错误信息。

### 结束时

- 运行 `pnpm run ts-check`。
- 运行相关 API smoke test。
- 用真实浏览器走一遍用户流程。
- 检查控制台、网络请求和数据库写入。
- 记录已完成、未完成和下周期第一项任务。

## 当前进度与下一步

当前不直接进入原阶段 5-8 的功能扩展。下一周期先执行内测计划的阶段 A：完成 staging/真实凭据的主链路验收，冻结首批内测范围与成本上限；随后执行阶段 B 的资格和额度控制。支付仅在阶段 C 建立领域模型、适配接口与 `manual_beta` 状态机，待内测数据足以决定渠道和定价后再启用真实支付。

阶段 2 的代码链路和回归测试已完成，真实 AI 解析使用 Alibaba 配置，远程数据库迁移按 Supabase 迁移文件执行。阶段 3 已开始，第一轮先保证匹配结果可信：

1. 已完成 `ai_matches` 的画像版本、评分维度、证据和关键差距快照。
2. 已完成 AI Match 严格 JSON 校验，AI 失败时不再生成随机分数。
3. 已完成已确认画像筛选、错误重试和证据/差距展示。
4. 已完成阶段三契约回归测试，见 `pnpm run test:phase3`。
5. 下一步接入真实岗位，验证 `0009_ai_match_profile_snapshots.sql` 迁移后的完整链路。
6. AI 主链路使用 Alibaba 文本模型，下一步开发岗位搜索排序、收藏/忽略和目标岗位简历评分。

阶段三的岗位详情页目标岗位评分入口已完成：用户可选择已确认简历，读取当前画像版本的历史评分或重新评分，并查看评分维度、证据、关键差距和优化建议；岗位详情页也已接入收藏状态。下一步进入阶段四简历优化闭环，同时补做真实 AI、远程迁移和登录态浏览器验收。

阶段四已完成第一轮服务端闭环：优化结果不再只存浏览器 `localStorage`，新增 `resume_optimizations` 版本表和 RLS，结果绑定目标岗位与确认画像版本，支持服务端历史读取、编辑保存和删除；优化接口对模型 JSON 做严格校验。当前仍需执行 `0011_resume_optimizations.sql` 并配置真实 AI，随后补评分对比、逐条建议操作和投递前确认。

阶段四已补充优化前后评分对比：`POST /api/ai/optimize/score` 使用同一岗位和六项维度评估原始/优化简历，保存分数变化和关键变化，优化页可直接触发并恢复该结果。阶段四又完成了逐条变更审核：AI 原始结构化结果不可变，用户拒绝的变更会恢复对应字段，撤销决定会重新计算审核结果。下一步是优化版本确认、导出排版验收和真实凭据下的完整浏览器验收。
